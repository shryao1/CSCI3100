/**
 * ListOptionsMenu.js
 *
 * Description: Array containing options for the dropdown menu in the MenuDown component.
 * 
 * Format:
 * Each item in the array represents a menu option and has the following structure:
 * - label: The label text displayed for the menu option.
 * 
 * Example:
 * const ListOptionsMenu = [
 *   { label: 'Posts' },
 *   { label: 'Favorites' }
 * ];
 * 
 * Usage:
 * This constant can be imported and used in the MenuDown component to populate the dropdown menu.
 */

export const ListOptionsMenu = [
	{ label: 'Posts' },
	{ label: 'Favorites' }
]